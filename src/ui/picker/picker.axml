<picker
id="{{Id}}"
name="{{name}}"
class="onekit-picker {{Class}}"
style="{{Style}}"
title="{{headerText}}"
range="{{range}}"
disabled="{{disabled}}"
onChange="picker_Change"
>
<slot>
</slot>
</picker>