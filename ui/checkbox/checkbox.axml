<view>
<checkbox
  id="{{onekitId}}"
  class="onekit-checkbox {{onekitClass}}"
  style="{{onekitStyle}}"
  name="{{name}}"
  value="{{value}}" 
  disabled="{{disabled}}"
  checked="{{checked}}"
  color="{{color}}"
  onChange="checkbox_Change"/>
<slot/>
</view>