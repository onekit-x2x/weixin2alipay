<progress
id="{{Id}}"
class="onekit-progress {{Class}}" 
style="{{Style}}"
percent="{{percent}}"
show-info="{{showInfo}}"
stroke-width="{{strokeWidth}}"
active-color="{{color}}"
background-color="{{backgroundColor}}"
active="{{active}}"
>
<slot>
</slot>
</progress>