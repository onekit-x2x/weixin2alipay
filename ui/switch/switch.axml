<switch
id="{{id}}"
name="{{name}}"
class="onekit-switch {{onekitClass}}"
style=“"{{onekitStyle}}"
name="{{name}}"
checked="{{checked}}"
disabled="{{disabled}}"
color="{{color}}"
onChange="switch_Change">
<slot></slot>
</switch>