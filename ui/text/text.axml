<text
id="{{onekitId}}"
class="onekit-text {{onekitClass}}"
style="{{onekitStyle}}"
selectable="{{selectable}}"
space="{{space}}"
decode="{{decode}}"
user-select="{{userSelect}}"
>
<slot>
</slot>
</text>