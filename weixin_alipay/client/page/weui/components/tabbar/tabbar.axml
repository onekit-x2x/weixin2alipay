<view class="weui-tabbar {{extClass}}">
  
  <view a:for="{{list}}" a:key="index" data-index="{{index}}" onTap="tabChange" class="weui-tabbar__item {{index === current ? 'weui-bar__item_on' : ''}}">
    <view style="position: relative;display:inline-block;">
      <image src="{{current === index ? item.selectedIconPath : item.iconPath}}" class="weui-tabbar__icon"></image>
      <mp-badge a:if="{{item.badge || item.dot}}" content="{{item.badge}}" style="position: absolute;top:-2px;left:calc(100% - 3px)"></mp-badge>
    </view>
    <view class="weui-tabbar__label">{{item.text}}</view>
  </view>
</view>